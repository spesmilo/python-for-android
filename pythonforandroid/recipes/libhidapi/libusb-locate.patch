--- a/libusb/CMakeLists.txt	2023-01-09 11:31:33.000000000 +0000
+++ b/libusb/CMakeLists.txt	2023-05-10 12:13:07.084522923 +0000
@@ -8,13 +8,10 @@
 )
 target_link_libraries(hidapi_libusb PUBLIC hidapi_include)

-if(TARGET usb-1.0)
-    target_link_libraries(hidapi_libusb PRIVATE usb-1.0)
-else()
-    include(FindPkgConfig)
-    pkg_check_modules(libusb REQUIRED IMPORTED_TARGET libusb-1.0>=1.0.9)
-    target_link_libraries(hidapi_libusb PRIVATE PkgConfig::libusb)
-endif()
+include_directories($ENV{WITH_LIBUSB})
+message(STATUS "libusb from $ENV{WITH_LIBUSB}")
+target_link_directories(hidapi_libusb PRIVATE $ENV{WITH_LIBUSB}/.libs)
+target_link_libraries(hidapi_libusb PRIVATE usb-1.0)

 find_package(Threads REQUIRED)
 target_link_libraries(hidapi_libusb PRIVATE Threads::Threads)
